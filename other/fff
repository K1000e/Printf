int	ft_print_u(unsigned int number)
{
	int i;
	char *base;

    i = 0;
	base = "0123456789";
	if (number > 10)
	    i += ft_print_u(number / 10);
	i += ft_print_char(*(base + (number % 10)));
	return (i);
} 
